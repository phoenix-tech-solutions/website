// TODO(confirm): do not publish names, roles, headshots, or pronouns until the roster is approved.
export const team: {
  slug: string;
  name: string;
  role: string;
  pronouns?: string;
  photo?: string;
  bio?: string;
}[] = [];
